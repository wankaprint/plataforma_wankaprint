/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['YOUR_SUPABASE_PROJECT_ID.supabase.co'], // User needs to update this
    },
};

export default nextConfig;
